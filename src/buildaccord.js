_ = require('lodash');
accord = require('accord');
npm = require('enpeem');

// Maintain supported packages list manually
// packages = [];
// _.each(accord.all(), function(obj, engine) {
// 	packages.push(accord.name_to_adapter(engine));
// 	packages.push('babel-preset-es2015');
// });
// npm.install({
// 	dependencies: packages
// 	, loglevel: 'warn'
// });

